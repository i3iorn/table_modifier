"""GUI package for table_modifier.

This package contains GUI-related subpackages and modules, such as the
main application window and its screens. Keep imports light here to
avoid side effects during package initialization.
"""

# Explicit re-exports for a clean public API.
# Avoid eager imports here; submodules should be imported where needed.
__all__: tuple[str, ...] = ()

